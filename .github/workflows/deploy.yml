name: Deploy Backend to EC2

on:
  push:
    branches:
      - main
    paths:
      - 'mission_vardi_backend/**'

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup and Restart Backend
        run: |
          # The runner is already inside your EC2 instance, so we just enter the backend directory
          cd mission_vardi_backend
          
          # Setup python environment
          python3 -m venv .venv
          source .venv/bin/activate
          
          # Install dependencies
          pip install --upgrade pip
          pip install -r requirements.txt
          
          # Start or restart using ecosystem.config.js
          # startOrRestart handles both: fresh start (no process) and subsequent restarts
          pm2 startOrRestart ecosystem.config.js --update-env
          pm2 save
          
          echo "Deployment successful!"
